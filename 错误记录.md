一：安欣遇到的错误：
ERROR: root:Error initializing main faster whisper transcription model: Repo id must be in the form 'reponame' or 'namespace/repo_name':'/home/a123/Desktop/DLab/test/5. 1.6/openpi_2/Embodied-AI-Brain/Models/faster-whisper-large-v3-turbo'. Use 'repo_ type' argument if needed.
在stt_server.py里，主程序初始化模型的model_path中，"Model"里的M大写了，导致找不到本地模型，faster-whisper认作传入的是repo_id 字符串触发 huggingface.
